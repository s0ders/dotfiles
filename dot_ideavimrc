" Note: if the .ideavimrc file is not loaded by the IdeaVim plugin, go into the IDE
" property file at <IDE_PATH>/bin/idea.properties and add "idea.vim.configuration.path=${user.home}/.ideavimrc".
" If issue persist, check that the file does not contain "ghost" characters using "cat -A .ideavimrc".

nnoremap <Space> <Nop>
map <Space> <Leader>

vnoremap < <gv
vnoremap > >gv

" Switch tabs
nnoremap <tab> <Action>(NextTab)
nnoremap <S-tab> <Action>(PreviousTab)

map <Leader>ff <Action>(SearchEverywhere)

map <Leader>gb <Action>(Annotate)

map <Leader>h <Action>(ActivateTerminalToolWindow)